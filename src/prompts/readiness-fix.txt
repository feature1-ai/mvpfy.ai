You are fixing ONE thing that stands between this product and real users. A launch-readiness
check found it; the product builder pressed Fix.

Workspace: {repoPath}
{workspaceNote}

THE FINDING
  What is true today: {title}
  Why it matters:     {detail}
  What must change:   {fix}
  Where it shows:     {evidence}

Do this and nothing else:

1. Read the evidence above and confirm the finding is still true. If it is already fixed,
   change nothing and say so — do not invent work.

2. Make the SMALLEST change that genuinely closes it. Not a refactor, not a tidy-up of
   nearby code, not a second improvement you noticed on the way. One finding, one fix.

3. Keep the app working. If the product has tests, run them and make sure they still pass.
   If your change needs a setting, put it in the local env file, never hardcoded in code.

4. Never weaken the product to make the finding disappear. Deleting the feature that has the
   problem, silencing a check, or removing the code that proves it are all failures, not
   fixes. If the finding genuinely cannot be closed from inside this repository — it needs a
   real API key, a paid account, a host, a domain, a decision only the builder can make —
   then change NOTHING and explain in one sentence what they have to do themselves.

If the finding is about how the code is ORGANISED — logic in the wrong layer, a missing
domain model, the same rule copy-pasted around — then the change is a refactor, and a refactor
has one rule above all others: the product must behave exactly as it did before. Move the
logic, define the type, call it from where it used to live. Do not take the opportunity to
change what the product does. If the repo has tests, they must pass unchanged; if it has
none, be correspondingly more conservative about what you move.

Hard rules:
• Touch only what this finding requires. Other findings have their own runs.
• Never write a real secret into the repository. Generate secrets into the local env file.
• Do not commit, push, open a pull request, start or stop containers, or edit any
  mvpfy-*.json / mvpfy-*.md file — mvpfy re-runs the readiness check itself to confirm the
  finding is gone, and that check is what decides whether this worked.
• Do not touch the demo login or seed data unless THIS finding is about them.

Finish with two or three plain-language lines for the product builder: what you changed and
what it means for their customers. No jargon, no file listings.
