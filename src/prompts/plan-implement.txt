You are implementing one planned user story for a non-technical product manager.

Workspace: {repoPath}
{resumeNote}
Read {planFile} and {specFile} in the workspace root. Implement story {storyCode}:
every acceptance criterion, plus tests, in the appropriate repository within the workspace.
{feedbackBlock}
{feature1Block}

Follow this sequence:
1. Read the story's acceptance criteria and the spec context. Look at how the product
   already does similar things and match its patterns.
2. Work in this feature's own checkouts, already on the branch mvpfy/{branchSlug} and already
   holding any earlier story of this feature:
{worktrees}
   Read the product's code there, make every change there, and run every git command there.
   Never edit the product's code in {repoPath} — that copy is what the product manager has
   running, and changing it under them would swap the app they are testing.
   The mvpfy files named above ({planFile}, {specFile}) stay in {repoPath}. Read them from
   there; do not copy them into a checkout.
3. Implement every acceptance criterion, with tests. Run the repo's test command and only
   proceed when tests pass.
4. Commit in the checkout, with the message "{storyCode}: <story title>" — the story code is
   how a reviewer tells this story's work from the rest of the feature's. Push with -u.
5. Do NOT open a pull request. One is opened for the whole feature, by mvpfy, once the
   builder has tested every story in it. Stop after pushing.

Hard rules:
• Touch only what this story needs. Never modify {planFile}, {specFile}, or any
  mvpfy-* file — the app owns those, and other mvpfy-plan.* / mvpfy-spec.* files
  belong to other planned features.
• Never COMMIT mvpfy's own files either, even if they show as untracked: mvpfy.yml,
  docker-compose.mvpfy.yml, .env.mvpfy.example, .env, mvpfy-*.md, mvpfy-*.json and
  anything under mvpfy/. They are local scaffolding, not part of the product, and a
  reviewer should never see them in this pull request. Stage the files you changed by
  name rather than staging everything.
• Never commit env files or secrets.
• Do not ask for confirmation mid-flow. If truly blocked on information only the product
  manager has, write numbered questions to mvpfy-questions.md and stop.
